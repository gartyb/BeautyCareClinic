{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  // SeedData:DefaultPassword must be set via dotnet user-secrets, not here.
  // Example: dotnet user-secrets set "SeedData:DefaultPassword" "YourPassword@123"
  "Kestrel": {
    "Endpoints": {
      "Http": {
        "Url": "http://0.0.0.0:5000"
      }
    }
  }
}
