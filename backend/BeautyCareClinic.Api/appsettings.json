{
  // IMPORTANT: The Jwt:Secret below is a placeholder and MUST NOT be used in any environment.
  // Set the real secret via dotnet user-secrets in Development:
  //   dotnet user-secrets set "Jwt:Secret" "<your-secret-here>"
  // The application will throw a clear error on startup if this placeholder is detected.
  //
  // IMPORTANT: ConnectionStrings:DefaultConnection MUST NOT be committed with credentials.
  // Set it via dotnet user-secrets in Development:
  //   dotnet user-secrets set "ConnectionStrings:DefaultConnection" "<your-connection-string>"
  // Or via the environment variable: ConnectionStrings__DefaultConnection
  "ConnectionStrings": {
    "DefaultConnection": ""
  },
  "Jwt": {
    "Secret": "REPLACE_WITH_USER_SECRETS_IN_DEV",
    "Issuer": "BeautyCareClinic",
    "Audience": "BeautyCareClinic",
    "ExpiresInHours": 24
  },
  "AllowedHosts": "*"
}
