2026-07-13 Phase 001 — Foundation + Customer View
  Status: Implementation complete, awaiting user validation
  Branch: feature/phase-001-customer-view
  Build: PASS (npm run build — 0 errors)
  Tests: 43 PASS, 0 FAIL
    - src/features/customer/selectors.test.ts: 27 tests
    - src/data/customersService.test.ts: 10 tests
    - src/features/search/SearchScreen.test.tsx: 6 tests

2026-07-13 Review Fixes — Security, correctness, and quality fixes
  Branch: feature/phase-001-customer-view
  Build: PASS (npm run build — 0 errors, 0 type errors)
  Tests: 45 PASS, 0 FAIL (+2 new tests)
    - src/features/customer/selectors.test.ts: 29 tests
    - src/data/customersService.test.ts: 10 tests
    - src/features/search/SearchScreen.test.tsx: 6 tests
  Fixes applied:
    FIX-1: vitest bumped from ^2.1.8 to ^3.2.6 (CVE GHSA-5xrq-8626-4rwp resolved, 0 vulnerabilities)
    FIX-2: tailwind.config.js — added zIndex.60 to theme.extend (z-60 now valid)
    FIX-3: CustomerCard.tsx — added isLoading state to prevent "not found" flash on direct URL load
    FIX-4: selectors.ts previousAppointment — filter changed to status === 'Completed'
    FIX-5: SearchResults.tsx — removed inline getLastAppointment, imported previousAppointment from selectors
    FIX-6: docs/adr/ADR-001 — updated completedTreatmentsForSeries signature to single-arg form
    FIX-7: selectors.test.ts — added test for past Scheduled exclusion; added floating point sum test
           selectors.ts outstandingBalance — rounds result to 2 decimal places
    FIX-8: package.json — removed @radix-ui/react-dialog and tailwind-merge (unused)
           npm install: 0 vulnerabilities, 348 packages
    FIX-9: TimerPanel.tsx — wired useActiveTimer context; displays elapsedSeconds as MM:SS or H:MM:SS
           and status label (פעיל/מושהה/לא פעיל); buttons call context no-ops
    FIX-10: SearchScreen.tsx — added maxLength={100} to search input
            customersService.ts — early return [] when query.length > 100
    FIX-11: Mock data comment added to all 8 data files
            therapists.ts — email domain changed from @beautyclinic.co.il to @example.co.il
  Files created:
    src/types/{User,Customer,TreatmentSeries,PackageType,Order,Payment,Treatment,Appointment,Note,TreatmentType,index}.ts
    src/data/{treatmentTypes,packageTypes,therapists,customers,series,orders,payments,treatments,appointments,notes,customersService}.ts
    src/contexts/{CustomerContext,ActiveTimerContext}.tsx
    src/features/customer/{selectors,CustomerCard,CustomerCardHeader,SummaryRow,QuickActionButtons,TimerPanel}.tsx
    src/features/customer/tabs/{ActiveSeriesTab,TreatmentHistoryTab,OrdersTab,PaymentsTab,NotesTab}.tsx
    src/features/search/{SearchScreen,SearchResults}.tsx
    src/components/shared/{Header,Sidebar,Badge,ProgressBar}.tsx
    src/App.tsx, src/main.tsx, src/index.css, src/vite-env.d.ts
    package.json, tsconfig.json, tsconfig.app.json, tsconfig.node.json
    vite.config.ts, vitest.config.ts, tailwind.config.js, postcss.config.js, index.html

2026-07-13 Phase 002 Review Fixes — Correctness, validation, and security fixes
  Branch: feature/phase-001-customer-view
  Build: PASS (npm run build — 0 TypeScript errors)
  Tests: 88 PASS, 0 FAIL (+10 new tests, all prior 78 green)
    - src/features/customer/selectors.test.ts: 38 tests (+9: openOrders x6, paymentsForOrder x3)
    - src/features/payment/paymentService.test.ts: 14 tests (+1: OVERPAYMENT throw replaces clamp)
    - src/features/order/orderService.test.ts: 15 tests (unchanged)
    - src/domain/errors.test.ts: 5 tests (unchanged)
    - src/data/customersService.test.ts: 10 tests (unchanged)
    - src/features/search/SearchScreen.test.tsx: 6 tests (unchanged)
  Fixes applied:
    FIX-1: CustomerContext.tsx — addPayment uses allOrdersRef to pre-compute updated order
           before any setState; DomainError now propagates to caller with no orphan payment
    FIX-2: selectors.ts openOrders — changed || to && (fully-paid orders no longer shown as open)
    FIX-3: NewOrderModal.tsx — added guard: if (selected.size === 0) return in handleSave
    FIX-4: NewOrderModal.tsx — maxOverride input: added max={24}, parseInt clamp in onChange,
           effectiveMax range guard in handleSave
    FIX-5: RecordPaymentModal.tsx — replaced loose isNaN check with AMOUNT_REGEX /^\d{1,7}(\.\d{1,2})?$/
           overpayment check uses toCents() for float-safe comparison
    FIX-6: paymentService.ts applyPaymentToOrder — throws DomainError('OVERPAYMENT') instead of clamping
           paymentService.test.ts — updated overpayment test to assert DomainError thrown (2 tests)
    FIX-7: RecordPaymentModal.tsx — removed stale initialOrderId variable; handleClose resets to
           preselectedOrderId ?? ''; auto-selection moved to useEffect reacting to available
    FIX-8: domain/money.ts toCents — throws DomainError('INVALID_AMOUNT') for NaN input
    FIX-9: OrdersTab.tsx — replaced inline payments.filter with paymentsForOrder() selector
    FIX-10: OrdersTab.tsx — replaced order.id.replace('order-','#') with #{sorted.length - index}
            (UUID-friendly numeric label, newest order = highest number)
    FIX-11: Modal.tsx — removed redundant onClick={onClose} from Dialog.Close button
    FIX-12: RecordPaymentModal.tsx — added date validation: rejects empty date and future dates
    FIX-13: NewOrderModal.tsx — added UX-only comment for server-side re-validation (CR-002)
  selectors.test.ts — added import for openOrders, paymentsForOrder, Payment type;
    added test blocks for openOrders (6 tests) and paymentsForOrder (3 tests)

2026-07-13 Phase 002 — New Order + Record Payment (write actions)
  Branch: feature/phase-002-new-order-payment
  Build: PASS (npm run build — 0 TypeScript errors)
  Tests: 78 PASS, 0 FAIL (+33 new tests, all prior 45 green)
    - src/domain/errors.test.ts: 5 tests (new)
    - src/features/order/orderService.test.ts: 15 tests (new)
    - src/features/payment/paymentService.test.ts: 13 tests (new)
    - src/features/customer/selectors.test.ts: 29 tests (unchanged)
    - src/data/customersService.test.ts: 10 tests (unchanged)
    - src/features/search/SearchScreen.test.tsx: 6 tests (unchanged)
  Files created:
    src/domain/{id,errors,constants,money}.ts
    src/features/order/{orderService,orderService.test,NewOrderModal}.ts/tsx
    src/features/payment/{paymentService,paymentService.test,RecordPaymentModal}.ts/tsx
    src/components/shared/{Modal,RoleGuard}.tsx
  Files updated:
    src/contexts/CustomerContext.tsx — mutable state (useState for orders/payments/series),
      addOrder() and addPayment() mutations exposed
    src/features/customer/selectors.ts — added openOrders() and paymentsForOrder()
    src/features/customer/QuickActionButtons.tsx — enabled New Order + Record Payment buttons,
      accepts currentUser and customerId props
    src/features/customer/tabs/OrdersTab.tsx — enabled per-order payment button with
      paymentCount < maxPaymentCount guard; accepts currentUser prop
    src/features/customer/CustomerCard.tsx — passes currentUser to QuickActionButtons and OrdersTab;
      CR-007 smart defaultTab based on activeSeries()
    src/features/customer/tabs/TreatmentHistoryTab.tsx — migrated to Modal wrapper (CR-003)
    src/features/customer/tabs/NotesTab.tsx — migrated to Modal wrapper (CR-003)
    src/App.tsx — CR-006 warning comment; passes currentUser to CustomerCard
    src/components/shared/Sidebar.tsx — CR-002 comment on role check
