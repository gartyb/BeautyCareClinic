2026-07-15 Phase 005 — New Customer, Package Admin, Therapist Management, Global Settings
  Branch: feature/phase-005-new-customer-admin
  Build: PASS (npm run build — 0 TypeScript errors)
  Tests: 195 PASS, 0 FAIL (+51 new tests, all prior 144 green)
    - src/features/customer/customerService.test.ts: 12 tests (new)
    - src/features/packages/packageTypeService.test.ts: 16 tests (new)
    - src/features/therapists/therapistDataService.test.ts: 14 tests (new)
    - src/features/settings/settingsService.test.ts: 7 tests (new)
    - src/features/order/orderService.test.ts: 15 tests (updated signature)
    - src/features/search/SearchScreen.test.tsx: 6 tests (updated providers + button state)
    - all prior 144 tests green
  Files created:
    src/contexts/CustomersContext.tsx — owns customer list, seed from data/customers.ts
    src/contexts/GlobalSettingsContext.tsx — owns defaultMaxPaymentCount
    src/data/globalSettings.ts — seed value defaultMaxPaymentCount: 3
    src/data/therapistWorkingHours.ts — empty seed array
    src/data/therapistUnavailableDates.ts — empty seed array
    src/data/therapistCapabilities.ts — empty seed array
    src/types/Therapist.ts — TherapistWorkingHours, TherapistUnavailableDate, TherapistCapability
    src/features/customer/customerService.ts — buildCustomer() with DomainError validation
    src/features/customer/customerService.test.ts — 12 unit tests
    src/features/customer/NewCustomerModal.tsx — modal with required name+phone, optional email
    src/features/packages/packageTypeService.ts — buildPackageType, updatePackageType, deletePackageType
    src/features/packages/packageTypeService.test.ts — 16 unit tests
    src/features/packages/PackageTypeModal.tsx — create/edit modal
    src/features/packages/PackagesScreen.tsx — CRUD list with manager guard
    src/features/therapists/therapistDataService.ts — working hours, unavailable dates, capabilities
    src/features/therapists/therapistDataService.test.ts — 14 unit tests
    src/features/therapists/TherapistsScreen.tsx — list Therapist-role users
    src/features/therapists/TherapistDetail.tsx — /therapists/:userId detail page
    src/features/settings/settingsService.ts — updateDefaultMaxPaymentCount with validation
    src/features/settings/settingsService.test.ts — 7 unit tests
    src/features/settings/SettingsScreen.tsx — defaultMaxPaymentCount form
    src/test/renderWithProviders.tsx — shared test helper with all 4 providers
  Files updated:
    src/main.tsx — provider order: BrowserRouter → GlobalSettings → Customers → Customer → ActiveTimer
    src/contexts/CustomerContext.tsx — uses useCustomers() instead of static customers import
    src/domain/constants.ts — DEFAULT_MAX_PAYMENT_COUNT kept for test backward compat (not used in runtime)
    src/features/order/orderService.ts — added defaultMaxPaymentCount param, removed constant import
    src/features/order/orderService.test.ts — updated all calls with new param, local constant
    src/features/order/NewOrderModal.tsx — reads defaultMaxPaymentCount via useGlobalSettings()
    src/components/shared/RoleGuard.tsx — added optional fallback prop (default null)
    src/components/shared/Sidebar.tsx — enabled packages/therapists/settings nav items with routes
    src/features/search/SearchScreen.tsx — wired לקוחה חדשה button to NewCustomerModal; uses useCustomers()
    src/App.tsx — added /packages, /therapists, /therapists/:userId, /settings routes with RoleGuard

2026-07-13 Phase 001 — Foundation + Customer View
  Status: Implementation complete, awaiting user validation
  Branch: feature/phase-001-customer-view
  Build: PASS (npm run build — 0 errors)
  Tests: 43 PASS, 0 FAIL
    - src/features/customer/selectors.test.ts: 27 tests
    - src/data/customersService.test.ts: 10 tests
    - src/features/search/SearchScreen.test.tsx: 6 tests

2026-07-13 Review Fixes — Security, correctness, and quality fixes
  Branch: feature/phase-001-customer-view
  Build: PASS (npm run build — 0 errors, 0 type errors)
  Tests: 45 PASS, 0 FAIL (+2 new tests)
    - src/features/customer/selectors.test.ts: 29 tests
    - src/data/customersService.test.ts: 10 tests
    - src/features/search/SearchScreen.test.tsx: 6 tests
  Fixes applied:
    FIX-1: vitest bumped from ^2.1.8 to ^3.2.6 (CVE GHSA-5xrq-8626-4rwp resolved, 0 vulnerabilities)
    FIX-2: tailwind.config.js — added zIndex.60 to theme.extend (z-60 now valid)
    FIX-3: CustomerCard.tsx — added isLoading state to prevent "not found" flash on direct URL load
    FIX-4: selectors.ts previousAppointment — filter changed to status === 'Completed'
    FIX-5: SearchResults.tsx — removed inline getLastAppointment, imported previousAppointment from selectors
    FIX-6: docs/adr/ADR-001 — updated completedTreatmentsForSeries signature to single-arg form
    FIX-7: selectors.test.ts — added test for past Scheduled exclusion; added floating point sum test
           selectors.ts outstandingBalance — rounds result to 2 decimal places
    FIX-8: package.json — removed @radix-ui/react-dialog and tailwind-merge (unused)
           npm install: 0 vulnerabilities, 348 packages
    FIX-9: TimerPanel.tsx — wired useActiveTimer context; displays elapsedSeconds as MM:SS or H:MM:SS
           and status label (פעיל/מושהה/לא פעיל); buttons call context no-ops
    FIX-10: SearchScreen.tsx — added maxLength={100} to search input
            customersService.ts — early return [] when query.length > 100
    FIX-11: Mock data comment added to all 8 data files
            therapists.ts — email domain changed from @beautyclinic.co.il to @example.co.il
  Files created:
    src/types/{User,Customer,TreatmentSeries,PackageType,Order,Payment,Treatment,Appointment,Note,TreatmentType,index}.ts
    src/data/{treatmentTypes,packageTypes,therapists,customers,series,orders,payments,treatments,appointments,notes,customersService}.ts
    src/contexts/{CustomerContext,ActiveTimerContext}.tsx
    src/features/customer/{selectors,CustomerCard,CustomerCardHeader,SummaryRow,QuickActionButtons,TimerPanel}.tsx
    src/features/customer/tabs/{ActiveSeriesTab,TreatmentHistoryTab,OrdersTab,PaymentsTab,NotesTab}.tsx
    src/features/search/{SearchScreen,SearchResults}.tsx
    src/components/shared/{Header,Sidebar,Badge,ProgressBar}.tsx
    src/App.tsx, src/main.tsx, src/index.css, src/vite-env.d.ts
    package.json, tsconfig.json, tsconfig.app.json, tsconfig.node.json
    vite.config.ts, vitest.config.ts, tailwind.config.js, postcss.config.js, index.html

2026-07-13 Phase 002 Review Fixes — Correctness, validation, and security fixes
  Branch: feature/phase-001-customer-view
  Build: PASS (npm run build — 0 TypeScript errors)
  Tests: 88 PASS, 0 FAIL (+10 new tests, all prior 78 green)
    - src/features/customer/selectors.test.ts: 38 tests (+9: openOrders x6, paymentsForOrder x3)
    - src/features/payment/paymentService.test.ts: 14 tests (+1: OVERPAYMENT throw replaces clamp)
    - src/features/order/orderService.test.ts: 15 tests (unchanged)
    - src/domain/errors.test.ts: 5 tests (unchanged)
    - src/data/customersService.test.ts: 10 tests (unchanged)
    - src/features/search/SearchScreen.test.tsx: 6 tests (unchanged)
  Fixes applied:
    FIX-1: CustomerContext.tsx — addPayment uses allOrdersRef to pre-compute updated order
           before any setState; DomainError now propagates to caller with no orphan payment
    FIX-2: selectors.ts openOrders — changed || to && (fully-paid orders no longer shown as open)
    FIX-3: NewOrderModal.tsx — added guard: if (selected.size === 0) return in handleSave
    FIX-4: NewOrderModal.tsx — maxOverride input: added max={24}, parseInt clamp in onChange,
           effectiveMax range guard in handleSave
    FIX-5: RecordPaymentModal.tsx — replaced loose isNaN check with AMOUNT_REGEX /^\d{1,7}(\.\d{1,2})?$/
           overpayment check uses toCents() for float-safe comparison
    FIX-6: paymentService.ts applyPaymentToOrder — throws DomainError('OVERPAYMENT') instead of clamping
           paymentService.test.ts — updated overpayment test to assert DomainError thrown (2 tests)
    FIX-7: RecordPaymentModal.tsx — removed stale initialOrderId variable; handleClose resets to
           preselectedOrderId ?? ''; auto-selection moved to useEffect reacting to available
    FIX-8: domain/money.ts toCents — throws DomainError('INVALID_AMOUNT') for NaN input
    FIX-9: OrdersTab.tsx — replaced inline payments.filter with paymentsForOrder() selector
    FIX-10: OrdersTab.tsx — replaced order.id.replace('order-','#') with #{sorted.length - index}
            (UUID-friendly numeric label, newest order = highest number)
    FIX-11: Modal.tsx — removed redundant onClick={onClose} from Dialog.Close button
    FIX-12: RecordPaymentModal.tsx — added date validation: rejects empty date and future dates
    FIX-13: NewOrderModal.tsx — added UX-only comment for server-side re-validation (CR-002)
  selectors.test.ts — added import for openOrders, paymentsForOrder, Payment type;
    added test blocks for openOrders (6 tests) and paymentsForOrder (3 tests)

2026-07-14 Phase 003 — Treatment Recording
  Branch: feature/phase-003-treatment-recording
  Build: PASS (npm run build — 0 TypeScript errors)
  Tests: 119 PASS, 0 FAIL (+31 new tests, all prior 88 green)
    - src/features/treatment/treatmentService.test.ts: 31 tests (new)
    - src/features/customer/selectors.test.ts: 38 tests (unchanged)
    - src/features/payment/paymentService.test.ts: 14 tests (unchanged)
    - src/features/order/orderService.test.ts: 15 tests (unchanged)
    - src/domain/errors.test.ts: 5 tests (unchanged)
    - src/data/customersService.test.ts: 10 tests (unchanged)
    - src/features/search/SearchScreen.test.tsx: 6 tests (unchanged)
  Files created:
    src/features/treatment/treatmentService.ts
    src/features/treatment/treatmentService.test.ts
  Files updated:
    src/contexts/ActiveTimerContext.tsx — replaced all no-ops with real timer logic:
      useState for targetSeriesId/isRunning/isPaused/elapsedSeconds,
      setInterval in a useEffect keyed on isRunning with cleanup on every transition,
      intervalRef in useRef; selectSeries blocked when timer active;
      endAndGetElapsed() atomically snapshots state then resets all fields
    src/contexts/CustomerContext.tsx — treatments moved from static import to useState
      (allTreatments + allTreatmentsRef mirror); customerTreatments derived from state;
      recordTimerTreatment() and recordQuantityTreatment() added with DomainError propagation;
      imported packageTypes, generateId, and treatmentService functions
    src/features/customer/TimerPanel.tsx — all buttons enabled with correct enable conditions;
      added "סיים טיפול" button calling endAndGetElapsed + recordTimerTreatment;
      accepts currentUser prop; shows active series name from packageTypes lookup
    src/features/customer/tabs/ActiveSeriesTab.tsx — action buttons wired:
      quantity series: "סמן טיפול כבוצע" calls recordQuantityTreatment;
      timer series: "התחל טיימר" calls selectSeries+start; disabled when timerBusy;
      accepts currentUser prop
    src/features/customer/CustomerCard.tsx — passes currentUser to ActiveSeriesTab and TimerPanel
    src/components/shared/Header.tsx — RTL navbar fix: clinic logo+name DOM-first (visual right),
      user-switcher DOM-second (visual left); dropdown now anchors to right-0

2026-07-13 Phase 002 — New Order + Record Payment (write actions)
  Branch: feature/phase-002-new-order-payment
  Build: PASS (npm run build — 0 TypeScript errors)
  Tests: 78 PASS, 0 FAIL (+33 new tests, all prior 45 green)
    - src/domain/errors.test.ts: 5 tests (new)
    - src/features/order/orderService.test.ts: 15 tests (new)
    - src/features/payment/paymentService.test.ts: 13 tests (new)
    - src/features/customer/selectors.test.ts: 29 tests (unchanged)
    - src/data/customersService.test.ts: 10 tests (unchanged)
    - src/features/search/SearchScreen.test.tsx: 6 tests (unchanged)
  Files created:
    src/domain/{id,errors,constants,money}.ts
    src/features/order/{orderService,orderService.test,NewOrderModal}.ts/tsx
    src/features/payment/{paymentService,paymentService.test,RecordPaymentModal}.ts/tsx
    src/components/shared/{Modal,RoleGuard}.tsx
  Files updated:
    src/contexts/CustomerContext.tsx — mutable state (useState for orders/payments/series),
      addOrder() and addPayment() mutations exposed
    src/features/customer/selectors.ts — added openOrders() and paymentsForOrder()
    src/features/customer/QuickActionButtons.tsx — enabled New Order + Record Payment buttons,
      accepts currentUser and customerId props
    src/features/customer/tabs/OrdersTab.tsx — enabled per-order payment button with
      paymentCount < maxPaymentCount guard; accepts currentUser prop
    src/features/customer/CustomerCard.tsx — passes currentUser to QuickActionButtons and OrdersTab;
      CR-007 smart defaultTab based on activeSeries()
    src/features/customer/tabs/TreatmentHistoryTab.tsx — migrated to Modal wrapper (CR-003)
    src/features/customer/tabs/NotesTab.tsx — migrated to Modal wrapper (CR-003)
    src/App.tsx — CR-006 warning comment; passes currentUser to CustomerCard
    src/components/shared/Sidebar.tsx — CR-002 comment on role check

2026-07-14 Phase 004 — Treatment Notes, Photos & Add Note
  Branch: feature/phase-003-treatment-recording
  Build: PASS (npm run build — 0 TypeScript errors)
  Tests: 144 PASS, 0 FAIL (+25 new tests, all prior 119 green)
    - src/features/treatment/treatmentService.test.ts: 47 tests (+7: buildTreatmentPhoto)
    - src/features/note/noteService.test.ts: 9 tests (new)
    - src/features/customer/selectors.test.ts: 38 tests (unchanged)
    - src/features/payment/paymentService.test.ts: 14 tests (unchanged)
    - src/features/order/orderService.test.ts: 15 tests (unchanged)
    - src/domain/errors.test.ts: 5 tests (unchanged)
    - src/data/customersService.test.ts: 10 tests (unchanged)
    - src/features/search/SearchScreen.test.tsx: 6 tests (unchanged)
  Files created:
    src/features/note/noteService.ts — pure buildNote() builder with injected deps
    src/features/note/noteService.test.ts — 9 unit tests for buildNote
    src/features/note/AddNoteModal.tsx — modal for customer-level quick-action note
    src/features/customer/tabs/TreatmentModal.tsx — extracted from TreatmentHistoryTab (R1)
      with note editing (הוסף הערה / ערוך הערה) and photo upload (הוסף תמונה)
  Files modified:
    src/features/treatment/treatmentService.ts — added buildTreatmentPhoto() pure builder (C3)
    src/features/treatment/treatmentService.test.ts — added 7 unit tests for buildTreatmentPhoto
    src/contexts/CustomerContext.tsx — notes moved to useState; added updateTreatmentNote,
      addTreatmentPhoto, addNote; TreatmentPhoto imported
    src/features/customer/tabs/TreatmentHistoryTab.tsx — refactored: imports TreatmentModal
      from its own file; old inline TreatmentModal removed
    src/features/customer/QuickActionButtons.tsx — "הוסף הערה" enabled, wired to AddNoteModal
  Architecture conditions implemented:
    C1: blob-URL session-scope documented in PHASE_SUMMARY.md Known Limitations
    C2: runtime guard if (!file.type.startsWith('image/')) return in TreatmentModal.tsx
    C3: buildTreatmentPhoto() in treatmentService.ts with unit tests; UI never builds TreatmentPhoto directly
    R1: TreatmentModal extracted to src/features/customer/tabs/TreatmentModal.tsx

2026-07-14 Phase 004 Review Fixes — Code Review + Security Review fixes
  Branch: feature/phase-003-treatment-recording
  Build: PASS (npm run build — 0 TypeScript errors)
  Tests: 146 PASS, 0 FAIL (+2 new tests, all prior 144 green)
    - src/features/note/noteService.test.ts: 11 tests (+2: DomainError on empty/whitespace)
    - all other test files unchanged
  Fixes applied:
    FIX-1: TreatmentModal.tsx — "מחק הערה" delete button (Trash2 icon) shown when note exists;
           CustomerContext.tsx updateTreatmentNote — empty string maps to notes: undefined (clears note)
    FIX-2: TreatmentModal.tsx — replaced static useState initialiser with useEffect syncing
           noteText to treatment.notes when prop changes
    FIX-3: TreatmentModal.tsx — useEffect cleanup revokes blob: URLs on unmount only
    FIX-4: TreatmentModal.tsx — (treatment.treatmentPhotos ?? []) null-guard on all accesses;
           treatments.ts confirmed all seeded treatments already have treatmentPhotos: []
    FIX-5: TreatmentModal.tsx — lightbox wrapped in ReactDOM.createPortal(…, document.body)
           to escape Radix Dialog stacking context
    FIX-6: noteService.ts — buildNote() throws DomainError('EMPTY_NOTE_TEXT') for empty/whitespace text;
           noteService.test.ts — 2 new tests for empty and whitespace-only inputs
    FIX-7: TreatmentModal.tsx — photo button + hidden file input moved outside ternary;
           always rendered alongside note buttons (not hidden when editingNote is true)
    FIX-8: TreatmentModal.tsx — ALLOWED_TYPES allowlist replaces startsWith('image/');
           10 MB size limit added; accept attribute tightened to match allowlist
    FIX-9: TreatmentModal.tsx — e.target.value = '' moved to top of handleFileChange,
           before type/size guards, so rejected files can be re-selected
    FIX-10: TreatmentModal.tsx + AddNoteModal.tsx — maxLength={2000} on both note textareas
  Files modified:
    src/features/customer/tabs/TreatmentModal.tsx
    src/features/note/noteService.ts
    src/features/note/noteService.test.ts
    src/features/note/AddNoteModal.tsx
    src/contexts/CustomerContext.tsx

2026-07-15 Phase 005 Code Review Fixes
  Branch: feature/phase-005-new-customer-admin
  Build: PASS (npm run build — 0 TypeScript errors)
  Tests: 199 PASS, 0 FAIL (+4 new tests: H4 price, H5 >24, H2 batch range, L2 assertions)
    - src/features/packages/packageTypeService.test.ts: 18 tests (+4: H4 PACKAGE_INVALID_PRICE x2, L2 expect.assertions on all validation tests)
    - src/features/settings/settingsService.test.ts: 8 tests (+1: H5 value>24; removed _current param; L2 expect.assertions)
    - src/features/therapists/therapistDataService.test.ts: 15 tests (+1: H2 batch range validation; L2 expect.assertions)
    - all prior 175 tests green
  Fixes applied:
    C1: CustomerContext.tsx — added customers to useMemo([activeCustomerId, customers]) dep array
    H1: Created src/contexts/PackageTypesContext.tsx — owns PackageType[] state, exposes createPackageType/updatePackageType/deletePackageType; seeded from data/packageTypes.ts
        Updated src/main.tsx — PackageTypesProvider inserted between CustomersProvider and CustomerProvider
        Updated PackagesScreen.tsx — consumes usePackageTypes() instead of local useState
        Updated NewOrderModal.tsx — consumes usePackageTypes() instead of static import
        Updated CustomerContext.tsx, TimerPanel.tsx, ActiveSeriesTab.tsx, TreatmentHistoryTab.tsx, OrdersTab.tsx, RecordPaymentModal.tsx — all now read from usePackageTypes() context
        Updated src/test/renderWithProviders.tsx — PackageTypesProvider added to test provider tree
    H2A: therapistDataService.ts — updateTherapistWorkingHours now delegates each row to buildWorkingHours, which throws DomainError('WORKING_HOURS_INVALID_RANGE') for invalid ranges
    H2B: TherapistDetail.tsx — handleSaveHours wrapped in try/catch; DomainError shown as inline hoursError message
    H3: TherapistDetail.tsx — hoursSuccessTimer and capsSuccessTimer stored in useRef; cleared before each new setTimeout; cleanup useEffect clears both on unmount
    H4: packageTypeService.ts — buildPackageType validates price via parseFloat; throws DomainError('PACKAGE_INVALID_PRICE') if NaN or negative
    H5: settingsService.ts — updateDefaultMaxPaymentCount throws if value > 24; _current param removed (L5)
        SettingsScreen.tsx — max={24} added to input; isValid updated to check parsed <= 24 (L3)
    M1: PackageTypeModal.tsx — isSeries checkbox handler replaced two sequential set() calls with single setForm() call
    M2: PackagesScreen.tsx — window.confirm replaced with inline deletingId state + confirm row ("למחוק את [name]?" with מחק/ביטול buttons)
    M3: TherapistDetail.tsx — useEffect([userId, workingHours]) changed to useEffect([userId]) to avoid overwriting unsaved edits; cleanup comment added
    M4: NewCustomerModal.tsx — handleSave wrapped in try/catch; saveError state drives inline error banner; DomainError imported
        CustomersContext.tsx — comment added documenting that DomainError propagates to caller
    M5: TherapistsScreen.tsx — comment documents that Managers are intentionally excluded from therapist list per domain model
    L1: TherapistsScreen.tsx ChevronLeft (forward/into detail) and TherapistDetail.tsx ChevronRight (back) already correct for RTL — confirmed, no change needed
    L2: packageTypeService.test.ts, settingsService.test.ts, therapistDataService.test.ts — expect.assertions(N) added to all try/catch throw tests; added expect(e).toBeInstanceOf(DomainError)
    L3: SettingsScreen.tsx — isValid rewritten using single parseInt call and Number.isNaN guard with <= 24 cap
    L5: settingsService.ts — removed unused _current: number parameter; SettingsScreen and tests updated

2026-07-16 Phase 006 — Appointment Calendar (complete)
  Branch: feature/phase-006-appointment-calendar
  Build: PASS (npm run build — 0 TypeScript errors)
  Tests: 252 PASS, 0 FAIL
  Features:
    1. Global settings: calendar start hour and end hour; calendar displays only those hours
    2. Appointment booking uses treatment types from the customer's active series (BookAppointmentModal pre-filters)
    3. Calendar shows day view with one column per therapist; click date label to open date picker
    4. Appointments tab on customer card; mark appointment as Completed or NoShow (toggle buttons, always visible)
  New features vs initial implementation:
    - AppointmentStatus extended with 'NoShow'
    - updateAppointmentStatus and rescheduleAppointment added to AppointmentsContext + service
    - RescheduleModal (3-step: date → therapist → time slot) with inline cancel confirmation
    - AppointmentsTab component: lists all customer appointments, status toggle buttons
    - CalendarGrid: z-index fix for multi-slot blocks; action buttons always visible at block bottom
    - Date picker via native <input type="date"> showPicker() positioned near date button
    - GlobalSettings extended with calendarStartHour + calendarEndHour; SettingsScreen form
    - BookAppointmentModal filters treatment types to active-series types when opened from customer card
  Files created:
    src/contexts/AppointmentsContext.tsx
    src/features/appointments/appointmentService.ts
    src/features/appointments/appointmentService.test.ts
    src/features/appointments/AppointmentCalendarScreen.tsx
    src/features/appointments/BookAppointmentModal.tsx
    src/features/appointments/AppointmentPopover.tsx
    src/features/appointments/RescheduleModal.tsx
    src/features/appointments/components/CalendarGrid.tsx
    src/features/customer/tabs/AppointmentsTab.tsx
    src/components/shared/Toast.tsx
    docs/adr/ADR-003-appointments-global-context.md
  Files modified:
    src/types/Appointment.ts — added durationMinutes, NoShow status
    src/types/TreatmentType.ts — added defaultDurationMinutes
    src/data/appointments.ts — added durationMinutes to all seed rows
    src/data/treatmentTypes.ts — added defaultDurationMinutes
    src/data/globalSettings.ts — added calendarStartHour, calendarEndHour
    src/contexts/TreatmentTypesContext.tsx — defaultDurationMinutes param
    src/contexts/GlobalSettingsContext.tsx — calendarStartHour, calendarEndHour
    src/contexts/CustomerContext.tsx — removed appointments; reads from AppointmentsContext
    src/features/customer/SummaryRow.tsx — next appointment from useAppointments()
    src/features/customer/QuickActionButtons.tsx — "קבע תור" wired to BookAppointmentModal
    src/features/customer/CustomerCard.tsx — Appointments tab added
    src/features/customer/selectors.test.ts — durationMinutes fixture fix
    src/features/search/SearchResults.tsx — appointments from useAppointments()
    src/features/settings/SettingsScreen.tsx — calendarStartHour + calendarEndHour fields
    src/features/treatmentTypes/TreatmentTypeModal.tsx — defaultDurationMinutes field
    src/utils/date.ts — formatDateTime added; formatDate fixed
    src/main.tsx — AppointmentsProvider added
    src/App.tsx — /appointments route
    src/components/shared/Sidebar.tsx — "לוח זמנים" nav item enabled
    src/test/renderWithProviders.tsx — AppointmentsProvider added
    vite.config.ts — port config

2026-07-15 Phase 006 — Appointment Calendar
  Branch: feature/phase-005-new-customer-admin
  Build: PASS (npm run build — 0 TypeScript errors)
  Tests: 252 PASS, 0 FAIL (+57 new tests, all prior 195 green)
    - src/features/appointments/appointmentService.test.ts: 40 tests (new)
    - src/features/customer/selectors.test.ts: 38 tests (+1 durationMinutes field fix)
    - all prior 212 tests green
  Files created:
    src/contexts/AppointmentsContext.tsx — global AppointmentsProvider; createAppointment, cancelAppointment
    src/features/appointments/appointmentService.ts — pure service: buildAppointment, cancelAppointmentInList, getAppointmentInterval, isSlotAvailable, getAvailableSlots, getAvailableTherapists, getNextAppointment
    src/features/appointments/appointmentService.test.ts — 40 unit tests
    src/features/appointments/AppointmentCalendarScreen.tsx — weekly RTL calendar, week navigation, today highlight
    src/features/appointments/BookAppointmentModal.tsx — 5-step (6 from calendar) booking wizard
    src/features/appointments/AppointmentPopover.tsx — appointment details + cancel with confirmation
    src/features/appointments/components/CalendarGrid.tsx — RTL CSS Grid weekly calendar
    src/components/shared/Toast.tsx — simple auto-dismiss toast component
  Files modified:
    src/types/Appointment.ts — added durationMinutes field, updated datetime comment
    src/types/TreatmentType.ts — added defaultDurationMinutes field
    src/data/appointments.ts — added durationMinutes to all 15 seed rows
    src/data/treatmentTypes.ts — added defaultDurationMinutes to all 3 seed entries
    src/contexts/TreatmentTypesContext.tsx — createTreatmentType/updateTreatmentType updated with defaultDurationMinutes param and validation
    src/contexts/CustomerContext.tsx — removed appointments array and derived selectors; removed static appointments import
    src/features/customer/SummaryRow.tsx — reads next appointment via useAppointments() + getNextAppointment(); formatDateTime shows dd/MM/yyyy HH:mm
    src/features/customer/QuickActionButtons.tsx — "קבע תור" button enabled, wired to BookAppointmentModal
    src/features/customer/selectors.test.ts — added durationMinutes to Appointment fixture
    src/features/treatmentTypes/TreatmentTypeModal.tsx — added "משך טיפול ברירת מחדל (דקות)" input
    src/features/search/SearchResults.tsx — reads appointments via useAppointments() context
    src/utils/date.ts — added formatDateTime (dd/MM/yyyy HH:mm); fixed formatDate to dd/MM/yyyy
    src/main.tsx — AppointmentsProvider inserted between PackageTypesProvider and CustomerProvider
    src/App.tsx — added /appointments route
    src/components/shared/Sidebar.tsx — "לוח זמנים" nav item enabled, routes to /appointments, visible to both roles
    src/test/renderWithProviders.tsx — AppointmentsProvider added to test provider tree

2026-07-17 Phase 008 Review Fixes — Security, correctness, and quality fixes
  Branch: feature/phase-007-backend-foundation
  Build: PASS (dotnet build — 0 errors; npm run build — 0 TypeScript errors)
  Tests: 252 PASS, 0 FAIL (frontend); 58 PASS, 0 FAIL (backend)
  Fixes applied:
    Fix 1 (P0): GlobalSettingsController.cs — removed null-guard blocking upsert; setting is now
      created with new Guid when absent; UpdateAsync upsert is now reachable
    Fix 1+12 (P0+P2): GlobalSettingsController.cs — bulk PUT loop wrapped in a DB transaction;
      AppDbContext injected; partial failures roll back all changes
    Fix 2 (P1/H3): appsettings.Development.json — SeedData:DefaultPassword removed; comment
      instructs use of dotnet user-secrets; DbSeeder.cs — missing password logs a warning and
      skips user seeding instead of falling back to a hardcoded password
    Fix 3 (P1): tokenManager.ts — getToken() now clears and returns null when token is expired
    Fix 4 (P1): apiClient.ts — auth:unauthorized event is suppressed when URL ends with /auth/login
    Fix 5 (P1): AuthController.cs — ExpiresInHours read via _configuration.GetValue<int>(…, 24)
      replacing int.Parse; IConfiguration injected as constructor parameter
    Fix 6 (P1): LoginPage.tsx — reads location.state?.from?.pathname via useLocation;
      navigates to saved path (or /search) after successful login;
      AuthContext.login no longer navigates (navigation delegated to caller)
    Fix 7 (H1): Program.cs — TokenValidationParameters.ValidAlgorithms pinned to HS256
    Fix 8 (H4): AuthController.cs — [ResponseCache(NoStore = true, …)] attribute on class;
      prevents proxy caching of auth responses
    Fix 9 (M2): Program.cs — explicit lockout policy: MaxFailedAccessAttempts=5,
      DefaultLockoutTimeSpan=15min, AllowedForNewUsers=true
    Fix 10 (L1): apiClient.ts — body removed from POST/PUT log statements
    Fix 11 (P2): TreatmentTypeRepository.cs — ExistsByNameAsync escapes \, %, _ in name
      before ILike; ESCAPE clause '\\' added
  Files changed:
    backend/BeautyCareClinic.Api/Controllers/GlobalSettingsController.cs
    backend/BeautyCareClinic.Api/Controllers/AuthController.cs
    backend/BeautyCareClinic.Api/appsettings.Development.json
    backend/BeautyCareClinic.Api/Program.cs
    backend/BeautyCareClinic.Infrastructure/Data/DbSeeder.cs
    backend/BeautyCareClinic.Infrastructure/Repositories/TreatmentTypeRepository.cs
    src/api/tokenManager.ts
    src/api/apiClient.ts
    src/contexts/AuthContext.tsx
    src/features/auth/LoginPage.tsx

2026-07-17 Phase 008 — Frontend-Backend Integration
  Branch: feature/phase-007-backend-foundation
  Build: PASS (npm run build — 0 TypeScript errors; dotnet build — 0 errors)
  Tests: 252 PASS, 0 FAIL (all 252 prior tests green + fetch mock setup)
    - 13 frontend test files: 252 tests (unchanged count; existing tests adapted to work with API-wired contexts)
    - Backend: 58 xUnit tests PASS (unchanged)
  Changes Closed: CR-008 (dev switcher removed), CR-011 (fullName migration), CR-015 (wildcard escaping)
  Files created (frontend):
    src/api/apiClient.ts — singleton fetch wrapper, Bearer auth, 401 → auth:unauthorized event
    src/api/apiError.ts — ApiError interface + ApiRequestError class
    src/api/tokenManager.ts — localStorage JWT with expiry tracking (authToken, authTokenExpiresAt)
    src/api/authApi.ts — loginUser(), getCurrentUser()
    src/api/customersApi.ts — CRUD /customers
    src/api/treatmentTypesApi.ts — CRUD /treatment-types
    src/api/usersApi.ts — CRUD /users
    src/api/globalSettingsApi.ts — GET + PUT /global-settings
    src/api/index.ts — re-exports
    src/contexts/AuthContext.tsx — currentUser, isInitializing, login, logout; auto-logout on 401 event
    src/components/shared/PrivateRoute.tsx — Outlet wrapper with initializing spinner + redirect
    src/features/auth/LoginPage.tsx — /login, RTL Hebrew, error mapping per HTTP code
    .env.example — VITE_API_URL=http://localhost:5000/api/v1
  Files created (backend migration):
    BeautyCareClinic.Infrastructure/Migrations/*_Phase008Changes.cs — TreatmentType.DefaultDurationMinutes + User.Phone
  Files modified (frontend):
    src/types/Customer.ts — fullName replaces firstName/lastName in CustomerSummary
    src/types/User.ts — fullName replaces firstName/lastName in User
    src/data/customers.ts — 5 customers updated to fullName
    src/data/therapists.ts — 2 users updated to fullName
    src/contexts/CustomersContext.tsx — API-wired; async CRUD; isLoading/error/refetch added
    src/contexts/TreatmentTypesContext.tsx — API-wired; async CRUD; isLoading/error added
    src/contexts/GlobalSettingsContext.tsx — API-wired; async setters; isLoading/error added; calendarHours setters async
    src/App.tsx — AuthProvider integration; /login public route; PrivateRoute wrapper; user switcher removed
    src/main.tsx — AuthProvider added inside BrowserRouter, outside other providers
    src/components/shared/Header.tsx — fullName from useAuth(); logout button; dev switcher removed
    src/features/search/SearchScreen.tsx — isLoading skeleton; error + retry; fullName filter
    src/features/customer/customerService.ts — no more name-splitting; returns fullName directly
    src/features/customer/CustomerCardHeader.tsx — initials from fullName; display fullName
    src/features/customer/NewCustomerModal.tsx — async handleSave; ApiRequestError error display
    src/features/therapists/TherapistModal.tsx — fullName single field (firstName/lastName collapsed)
    src/contexts/TherapistsContext.tsx — createTherapist(fullName, email, phone)
    src/features/settings/SettingsScreen.tsx — async save handlers with loading state
    src/features/treatmentTypes/TreatmentTypeModal.tsx — async handleSave with isSaving state
    src/features/treatmentTypes/TreatmentTypesScreen.tsx — async delete
    src/features/appointments/AppointmentPopover.tsx — customer.fullName
    src/features/appointments/RescheduleModal.tsx — therapist.fullName
    src/features/appointments/BookAppointmentModal.tsx — customer.fullName / therapist.fullName
    src/features/appointments/components/CalendarGrid.tsx — therapist.fullName
    src/features/customer/tabs/NotesTab.tsx — author fullName
    src/features/customer/tabs/TreatmentModal.tsx — therapist.fullName
    src/features/customer/tabs/PaymentsTab.tsx — fullName
    src/features/customer/tabs/TreatmentHistoryTab.tsx — fullName
    src/features/customer/tabs/AppointmentsTab.tsx — therapist.fullName
    src/data/customersService.ts — fullName property
    src/test/setup.ts — global fetch mock for API-wired contexts
    src/test/renderWithProviders.tsx — AuthProvider added
    src/features/customer/customerService.test.ts — fullName assertions
    src/data/customersService.test.ts — fullName property assertions
    src/features/order/orderService.test.ts — User fixture uses fullName
    src/features/payment/paymentService.test.ts — User fixture uses fullName
  Files modified (backend):
    BeautyCareClinic.Domain/Entities/TreatmentType.cs — DefaultDurationMinutes int?
    BeautyCareClinic.Domain/Entities/User.cs — Phone string?
    BeautyCareClinic.Application/DTOs/TreatmentTypeDtos.cs — DefaultDurationMinutes in all 3 records
    BeautyCareClinic.Application/DTOs/AuthDtos.cs — Phone? in UserDto
    BeautyCareClinic.Application/DTOs/UserDtos.cs — Phone? in Create/UpdateUserRequest
    BeautyCareClinic.Application/Services/GlobalSettingsKeys.cs — CalendarStartHour/CalendarEndHour keys + KnownKeys
    BeautyCareClinic.Api/Controllers/TreatmentTypesController.cs — DefaultDurationMinutes mapping
    BeautyCareClinic.Api/Controllers/UsersController.cs — Phone mapping (trim + null-coerce)
    BeautyCareClinic.Api/Controllers/AuthController.cs — Phone in UserDto
    BeautyCareClinic.Infrastructure/Repositories/CustomerRepository.cs — CR-015 wildcard escaping
    BeautyCareClinic.Infrastructure/Data/DbSeeder.cs — Phone + DefaultDurationMinutes + calendar settings seed
  Documentation updated:
    docs/ARCHITECTURE.md — Frontend structure (Phase 8), API Layer Pattern, State Management table
    docs/UI.md — Login screen added; Global Settings updated
    CHANGE_REQUESTS.md — CR-008/CR-011/CR-015 closed; Completed section updated
    phases/phase-008/PHASE_SUMMARY.md — implementation results, test results, lessons learned

2026-07-17 Phase 009 Review Fixes — Code review, security and correctness fixes
  Branch: feature/phase-009-orders-payments
  Build: PASS (dotnet build — 0 errors; npm run build — 0 TypeScript errors)
  Tests: 254 PASS, 0 FAIL (frontend, +2 new); 105 PASS, 0 FAIL (backend, +21 new)
  Fixes applied:
    C3 (CRITICAL): paymentService.ts applyPaymentToOrder — changed priority of total baseline:
      discountedPrice now takes precedence over originalPrice.
      Before: totalPrice ?? originalPrice ?? discountedPrice; a customer with 20% discount on
      1000₪ could be charged 900₪ because originalPrice=1000 was picked first.
      After: discountedPrice ?? totalPrice ?? originalPrice
      New frontend tests: 'uses discountedPrice (not originalPrice) as the total when both present',
      'throws OVERPAYMENT when payment exceeds discountedPrice (not originalPrice)'
    C2: TreatmentSeriesController.cs ListByCustomer — removed broken active query param.
      Both branches were calling GetActiveByCustomerIdAsync; ?active=false silently returned active results.
      Endpoint now always returns active series; active param removed entirely.
      XML doc updated to say "Returns active series only."
      treatmentSeriesApi.ts listActiveByCustomer — removed ?active=true query string (now implicit).
    C1: N+1 fix in CustomerOrdersController ListByCustomer.
      ICustomerOrderRepository — added GetPaymentCountsByOrderIdsAsync(IEnumerable<Guid>).
      CustomerOrderRepository — implemented via single GroupBy EF query (no loop).
      CustomerOrdersController.ListByCustomer — batch call replaces per-order loop.
      New backend test: GetPaymentCountsByOrderIds_ReturnsBatchCounts (3 orders, mixed counts).
      New backend test: GetPaymentCountsByOrderIds_ReturnsEmptyForNoOrders.
    S4: CustomerOrdersController.Create — added Items.Count > 50 guard returning 400.
    S1: PaymentsController.Create — payment method allowlist: Cash, Credit Card, Bank Transfer, Check, Other.
    S2: PaymentsController.Create — amount upper bound (> 99,999,999.99 → 400) and decimal
      precision guard (more than 2 places → 400).
    S3: PaymentsController.Create — PaymentDate bounds: future date → 400; older than 10 years → 400.
    C4: PackageTypesController.cs — changed all 3 English "The requested resource was not found."
      messages to Hebrew "סוג החבילה לא נמצא." in GetById, Update, Delete.
  New backend tests (21 total in Phase009Tests.cs):
    PaymentValidation_RejectsAmountAboveUpperBound
    PaymentValidation_RejectsMoreThanTwoDecimalPlaces (theory: 3 cases)
    PaymentValidation_AcceptsValidDecimalPrecision (theory: 3 cases)
    PaymentValidation_RejectsDisallowedPaymentMethod (theory: 4 cases)
    PaymentValidation_AcceptsAllowedPaymentMethods (theory: 5 cases)
    PaymentValidation_RejectsFutureDate
    PaymentValidation_RejectsDateOlderThan10Years
    PaymentValidation_AcceptsToday
    CustomerOrderRepository_GetPaymentCountsByOrderIds_ReturnsBatchCounts
    CustomerOrderRepository_GetPaymentCountsByOrderIds_ReturnsEmptyForNoOrders
  New frontend tests (2 in paymentService.test.ts):
    uses discountedPrice (not originalPrice) as the total when both are present
    throws OVERPAYMENT when payment exceeds discountedPrice (not originalPrice)
  Documentation updated:
    docs/WORKFLOWS.md — new section "Access Model and Trust Boundary (Phase 009)"
    CHANGE_REQUESTS.md — added CR-026 through CR-030 under Open

2026-07-17 Phase 010 — Treatment Recording & Notes
  Branch: feature/phase-010-treatment-recording-notes
  Build: PASS (dotnet build — 0 errors; npm run build — 0 TypeScript errors)
  Tests: 274 frontend PASS, 0 FAIL; 105 backend PASS (includes 21 new Phase010Tests), 0 FAIL

  Backend changes:
    Domain entities updated:
      Treatment.cs — added Notes (string?, nullable), PerformedByFullName (string, snapshot)
      Note.cs — added WrittenByFullName (string, snapshot)
    AppDbContext.cs — EF mappings for new columns; Note.Content MaxLength(5000)
    Migration: Phase010TreatmentNotes — adds notes, performed_by_full_name, written_by_full_name columns;
      alters Note.Content to varchar(5000)
    New interfaces:
      ITreatmentRepository (GetByIdAsync, ListByCustomerAsync, AddAsync, DeleteAsync)
      INoteRepository (GetByIdAsync, ListByCustomerAsync, AddAsync, UpdateAsync, DeleteAsync)
    New repositories:
      TreatmentRepository — eager-loads TreatmentType, orders by TreatmentDate DESC
      NoteRepository — eager-loads TreatmentType, orders by NoteDate DESC
    New DTOs:
      TreatmentDtos.cs — CreateTreatmentRequest, TreatmentDto
      NoteDtos.cs — CreateNoteRequest, UpdateNoteRequest, NoteDto
    New controllers:
      TreatmentsController — 4 endpoints; POST uses SELECT FOR UPDATE on TreatmentSeries;
        DELETE reverses series counters (clamped >= 0); Author/Manager permission check (RC-2, RC-3)
      NotesController — 5 endpoints; content validation (non-empty, <= 5000 chars, date <= today);
        PUT/DELETE: Author/Manager only (403 otherwise)
    Program.cs — registered ITreatmentRepository and INoteRepository
    New backend tests (21 in Phase010Tests.cs):
      Timer series increment, quantity series increment, clamp at cap, standalone treatment,
      future date validation (422), negative durationMinutes (422),
      delete reversal (timer and quantity), auth checks (403 wrong author, 204 manager),
      note create/update/delete, note content validation (empty, >5000, >today),
      DTO shape verification, edge cases

  Frontend changes:
    src/types/Treatment.ts — added userId, performedByFullName, treatmentTypeName (backward-compatible)
    src/types/Note.ts — added content, userId, writtenByFullName, noteDate, treatmentTypeName (backward-compatible)
    src/api/treatmentsApi.ts — new module: listByCustomer, getById, create, delete
    src/api/notesApi.ts — new module: listByCustomer, getById, create, update, delete
    src/api/index.ts — exports for treatmentsApi, notesApi, CreateTreatmentRequest, CreateNoteRequest, UpdateNoteRequest
    src/features/customer/tabs/TreatmentHistoryTab.tsx — getTherapistName helper (prefers performedByFullName)
    src/features/customer/tabs/NotesTab.tsx — fully rewritten: NoteModal (add/edit), NoteDetailModal,
      NoteRow with role-based edit/delete visibility, confirm delete dialog, error handling in Hebrew
    src/features/treatment/apiTreatmentTests.test.ts — 20 new frontend tests:
      treatmentsApi.create (2), treatmentsApi.delete (1), treatmentsApi.listByCustomer (1),
      treatmentsApi.getById (1), notesApi.create (1), notesApi.update (1), notesApi.delete (1),
      notesApi.listByCustomer (1), Note role visibility (3), Treatment role visibility (3),
      NoteModal validation (5)

  Architecture decisions enforced:
    ADR-010-A: SELECT FOR UPDATE on TreatmentSeries for all read-modify-write (RC-2)
    ADR-010-B: Author-or-Manager pattern — 403 inside action after entity load (RC-3)
    RC-6: DTO never accepts userId from client — always ICurrentUserService.GetCurrentUserId()
    RC-7: DateOnly for all date fields (consistent with Phase 009 PaymentDate)

  Documentation updated:
    docs/API_SPECIFICATION.md — 9 new endpoints for Treatments and Notes
    docs/DATABASE_SCHEMA.md — Treatment: notes, performed_by_full_name; Note: written_by_full_name, varchar(5000) content
    phases/phase-010/PHASE_SUMMARY.md — status updated to Implementation

2026-07-19 Bug fix — customer search shows "שגיאה בטעינת הנתונים" on first login
  Branch: (none — implementation only, not committed by request)
  Build: FAIL — pre-existing unrelated error: src/features/payment/RecordPaymentModal.tsx(18,53)
    TS6133 'currentUser' declared but never read (confirmed present on main before this fix
    via git stash + npx tsc -b; logged as FOLLOWUPS.md FU-016). No new build errors introduced.
  Tests: 274 PASS, 0 FAIL (npx vitest run --config vitest.config.ts) — all pre-existing tests
    still green, including src/features/search/SearchScreen.test.tsx (6 tests).
  Root cause: CustomersProvider, TreatmentTypesProvider, PackageTypesProvider and
    GlobalSettingsProvider each fired their API fetch unconditionally on mount
    (useEffect(() => { fetchX(); }, [fetchX])), before AuthProvider had a chance to restore
    a session / before login completed. The resulting 401 was captured as a permanent error
    state that never cleared after a successful login, because the effect had no dependency
    on auth state. TherapistDataContext, TherapistsContext and AppointmentsContext use local
    seed data only (no API calls), so they were not affected despite being named in the bug
    report as "check for the same pattern."
  Files updated:
    src/contexts/CustomersContext.tsx — fetch effect now gated on useAuth().currentUser;
      skips fetch (and clears isLoading) while unauthenticated, fetches once currentUser
      becomes non-null (covers both session-restore-on-reload and fresh-login)
    src/contexts/TreatmentTypesContext.tsx — same currentUser-gated fetch effect
    src/contexts/PackageTypesContext.tsx — same currentUser-gated fetch effect
    src/contexts/GlobalSettingsContext.tsx — same currentUser-gated fetch effect
    src/test/renderWithProviders.tsx — seeds a valid auth token (tokenManager.setToken) before
      render so AuthProvider restores an authenticated session in tests, matching the new
      gated-fetch behavior of the four providers above (setup.ts already mocks GET /auth/me)
  Not touched (by design — no API-fetching useEffect present):
    src/contexts/TherapistDataContext.tsx, TherapistsContext.tsx, AppointmentsContext.tsx
  Follow-ups logged:
    FOLLOWUPS.md FU-016 — pre-existing unrelated `npm run build` failure
      (RecordPaymentModal.tsx unused currentUser param), found while verifying this fix

2026-07-19 Bug fix follow-up — code-reviewer findings addressed (still uncommitted)
  Build: FAIL — same pre-existing unrelated TS6133 in RecordPaymentModal.tsx (FU-016), no new errors.
  Tests: 276 PASS, 0 FAIL (274 baseline + 2 new regression tests).
  Addressed:
    1. (High) Added a real regression test that exercises the fixed race directly, instead of
       only testing the "already authenticated" path:
       src/contexts/authGatedFetch.test.tsx — renders CustomersProvider / TreatmentTypesProvider
       trees unauthenticated (renderWithProviders(ui, { authenticated: false })), asserts zero
       fetch() calls to /customers /treatment-types and no error state while logged out, then
       drives a real AuthContext.login() transition and asserts the fetch fires and real data
       loads with no error afterward.
    2. (Medium) Added the same cancelled-flag guard GlobalSettingsContext already had to the
       other three auth-gated fetch effects, so a stale in-flight request from a prior auth
       state (e.g. logout -> re-login) can't resolve late and clobber newer state:
       src/contexts/CustomersContext.tsx (fetchCustomers takes an optional isCancelled check),
       src/contexts/TreatmentTypesContext.tsx (fetchAll takes an optional isCancelled check),
       src/contexts/PackageTypesContext.tsx (inline cancelled flag, matches GlobalSettingsContext).
    3. (Medium, test isolation) src/test/setup.ts — added afterEach(() => clearToken()) so seeded
       tokens don't bleed across tests; added a POST /auth/login mock response (MOCK_LOGIN_RESPONSE)
       so tests can drive a real login() call.
       src/test/renderWithProviders.tsx — added an `authenticated?: boolean` option (default true,
       so all existing callers are unaffected) that skips the setToken seeding when false.
  Deferred (per explicit instruction, not done):
    - Clearing stale customers/treatmentTypes/etc. arrays on the !currentUser branch (Low; not
      user-visible since all consumers are behind PrivateRoute).
    - Documenting CustomerContext.tsx (singular) review status in PROGRESS.txt (Low, doc-only).

2026-07-19 Bug fix — POST /api/v1/customers/{customerId}/orders always returned 500
  Branch: (none — implementation only, not committed by request)
  Root cause: `TreatmentSeries.CustomerId` was added to the Domain entity, Fluent config
    (AppDbContext.cs) and the EF model snapshot in the Phase 010 commit (ce37844), but the
    actual Phase 010 migration file (20260717160005_Phase010TreatmentNotes.cs) never emitted
    the corresponding AddColumn/CreateIndex/AddForeignKey for it. EF's __EFMigrationsHistory
    table listed all 5 migrations as applied, so `dotnet ef database update` was a no-op — the
    live Postgres "TreatmentSeries" table never actually had the column. Every order creation
    (which inserts a TreatmentSeries row with CustomerId set) threw at the DB layer -> 500.
    Confirmed via `\d "TreatmentSeries"` on beautycare-postgres (column absent) vs. the model
    snapshot (column present, indexed).
  Fix: because the model snapshot already matched the (correct) current code model, a plain
    `dotnet ef migrations add` would have produced an empty diff. Temporarily reverted the
    TreatmentSeries.CustomerId property/index/FK block in
    backend/BeautyCareClinic.Infrastructure/Migrations/AppDbContextModelSnapshot.cs to the
    pre-Phase-010 state, ran `dotnet ef migrations add AddTreatmentSeriesCustomerId`
    (--project Infrastructure --startup-project Api, ConnectionStrings__DefaultConnection env
    var set for the design-time factory), which generated
    backend/BeautyCareClinic.Infrastructure/Migrations/20260719085129_AddTreatmentSeriesCustomerId.cs
    and regenerated the snapshot — diffed byte-identical to the pre-edit original, confirming
    correctness. No backfill needed (`SELECT COUNT(*) FROM "TreatmentSeries"` was 0 before the
    fix). Applied with `dotnet ef database update` against the live dev DB (backend process on
    :5000 picked it up on the next request with no restart, per EF's per-request DbContext).
  Migration (Up): AddColumn<Guid> CustomerId (uuid, not null, default all-zero Guid — table was
    empty so this is inert) -> CreateIndex IX_TreatmentSeries_CustomerId -> AddForeignKey
    FK_TreatmentSeries_Customers_CustomerId (Customers.Id, ON DELETE RESTRICT). Down reverses
    in the opposite order (drop FK, drop index, drop column).
  Verified end-to-end: logged in as manager@clinic.local, GET /customers and GET /package-types
    for real IDs, then POST /api/v1/customers/{id}/orders with one item ->
    HTTP 201, body includes items[0].seriesId; confirmed via SQL that the inserted
    TreatmentSeries row has the correct CustomerId populated.
  Frontend: reviewed src/features/order/NewOrderModal.tsx and CustomerContext.tsx addOrder —
    payload shape (discountPercentage, maxPaymentCount, items[].packageTypeId) already matches
    CreateOrderRequest exactly. No frontend bug, no frontend changes made.
  Build: backend Domain/Application/Infrastructure/Api projects build clean.
    BeautyCareClinic.Tests still fails to compile — pre-existing unrelated error, already
    logged as FOLLOWUPS.md FU-015 (DateOnly/DateTime mismatch in Phase010Tests.cs:592), not
    touched per explicit instruction. No regression test added to that project as a result;
    logged as new followup instead.
  Files changed:
    backend/BeautyCareClinic.Infrastructure/Migrations/20260719085129_AddTreatmentSeriesCustomerId.cs (new)
    backend/BeautyCareClinic.Infrastructure/Migrations/20260719085129_AddTreatmentSeriesCustomerId.Designer.cs (new)
    backend/BeautyCareClinic.Infrastructure/Migrations/AppDbContextModelSnapshot.cs (regenerated
      by `dotnet ef migrations add`; content unchanged vs. pre-fix, confirmed via diff)
    docs/DATABASE_SCHEMA.md — added TREATMENT_SERIES.customer_id FK to the ER diagram/table and
      a note explaining the denormalization rationale
  Follow-ups logged:
    FOLLOWUPS.md FU-017 — regression test for CustomerOrdersController.Create covering
      TreatmentSeries.CustomerId is blocked by FU-015 (Tests project doesn't compile)

2026-07-19 Bug fix follow-up — code-reviewer findings addressed (still uncommitted)
  Branch: (none — implementation only, not committed by request)
  Build: PASS — `dotnet build BeautyCareClinic.sln` succeeds with 0 errors across all 5 projects
    (Domain/Application/Infrastructure/Api/Tests), a change from the prior session where Tests
    could not compile.
  Tests: 130 PASS, 0 FAIL (`dotnet test BeautyCareClinic.Tests/BeautyCareClinic.Tests.csproj`,
    with ConnectionStrings__DefaultConnection set to the dev Postgres — 129 pre-existing +
    1 new real-Postgres integration test).
  Addressed (per code-reviewer + user-approved follow-ups):
    1. (Medium) Migration lacked a backfill path for pre-existing TreatmentSeries rows.
       backend/BeautyCareClinic.Infrastructure/Migrations/20260719085129_AddTreatmentSeriesCustomerId.cs
       — added a migrationBuilder.Sql(...) UPDATE between AddColumn and CreateIndex/AddForeignKey
       that backfills CustomerId via OrderItems -> CustomerOrders -> CustomerId for any row that
       already existed. Verified for real: rolled the DB back to the pre-migration schema (there
       were, by then, 3 real TreatmentSeries rows left over from earlier live verification calls
       with no CustomerId), reapplied the updated migration, and confirmed via SQL that all 3
       rows were correctly backfilled to match their order's actual customer. Re-verified a live
       POST /api/v1/customers/{id}/orders still returns 201 after this change.
    2. (High) Fixed FOLLOWUPS.md FU-015 — the one compile error blocking the whole Tests
       project. backend/BeautyCareClinic.Tests/Application/Phase010Tests.cs:592 — changed
       `TreatmentDate: DateOnly.FromDateTime(DateTime.UtcNow)` to `TreatmentDate: DateTime.UtcNow`,
       matching TreatmentDto's actual `DateTime TreatmentDate` field and every other
       TreatmentDate usage already in that file. No other lines touched.
    3. (High) Added the real-Postgres regression test FOLLOWUPS.md FU-017 called for:
       backend/BeautyCareClinic.Tests/Integration/CustomerOrdersControllerPostgresTests.cs (new).
       Opens a real Npgsql AppDbContext (ConnectionStrings__DefaultConnection env var, matching
       the existing DesignTimeDbContextFactory convention), calls Database.MigrateAsync() to
       apply real migrations, invokes CustomerOrdersController.Create directly with real
       repositories against self-contained seed data (a Customer/TreatmentType/PackageType
       created and torn down by the test itself), and asserts both a 201 CreatedAtActionResult
       and — via a fresh AsNoTracking() query straight from Postgres — that the persisted
       TreatmentSeries row has the correct CustomerId. Proved this test actually catches the
       original bug class: temporarily removed the new migration's .cs/.Designer.cs files from
       the build and rolled the DB back to the pre-fix schema, re-ran just this test, and it
       failed with `Npgsql.PostgresException 42703: column "CustomerId" of relation
       "TreatmentSeries" does not exist` — the identical underlying error to the original
       production 500. Restored the migration files and DB state (dotnet ef database update)
       and confirmed the full suite is green again (130/130) and a live POST still returns 201.
  Files changed:
    backend/BeautyCareClinic.Infrastructure/Migrations/20260719085129_AddTreatmentSeriesCustomerId.cs
      — added backfill Sql() step (see #1 above)
    backend/BeautyCareClinic.Tests/Application/Phase010Tests.cs — one-line DateOnly -> DateTime fix
    backend/BeautyCareClinic.Tests/Integration/CustomerOrdersControllerPostgresTests.cs (new)
  FOLLOWUPS.md: FU-015 and FU-017 marked RESOLVED with details of the fix/verification.
  Not touched: env.development.local, env.example, zipcheck/ (per explicit constraint).
  Still not committed/tagged, per explicit instruction.

2026-07-19 CR-031 — Stable per-customer package numbering
  Type: Feature (approved via pm-spec + architect-review with 5 corrections RC-1..RC-5, all applied)
  Scope: Add a persistent OrderItem.PackageNumber, assigned once per customer at order-creation
  time in purchase order, so ActiveSeriesTab and TreatmentHistoryTab on the Customer Card show
  the same "#N" for the same package instead of each tab computing its own idx+1.
  Build: PASS — dotnet build BeautyCareClinic.sln (5 projects, 0 errors, 0 warnings).
  Backend tests: PASS — dotnet test BeautyCareClinic.sln with ConnectionStrings__DefaultConnection
    set to the dev Postgres — 131/131 (129 pre-existing + 2 new, one extending
    CustomerOrdersControllerPostgresTests.cs for multi-item + multi-order numbering).
  Frontend: npx tsc -b --noEmit — 1 pre-existing unrelated error (RecordPaymentModal.tsx unused
    currentUser param, already tracked as FOLLOWUPS.md FU-016, confirmed present on main via
    git stash before this change). npx vitest run --config vitest.config.ts — 276/276 passing,
    0 failures (no regressions).
  Design (per architect-review corrections):
    RC-1 — Concurrency: CustomerOrdersController.Create now takes a row-level lock on the
      Customer row (SELECT "Id" FROM "Customers" WHERE "Id" = {0} FOR UPDATE) inside the existing
      transaction before computing COALESCE(MAX(oi."PackageNumber"),0) across the customer's
      OrderItems (joined through CustomerOrders), then assigns base+1, base+2, ... across the
      request's item loop. Matches the exact SqlQueryRaw FOR UPDATE pattern already used by
      PaymentsController (ADR-010-A). Live-verified: 5 concurrent POSTs for the same customer
      returned distinct sequential numbers (4,5,6,7,8) with zero collisions.
    RC-2 — No unique index added (can't go on OrderItems — no CustomerId column there — and is
      unnecessary once the row lock serializes assignment).
    RC-3/RC-4 — Migration 20260719105258_AddPackageNumberToOrderItems.cs: (1) AddColumn as
      NULLABLE, (2) Sql(...) backfill using ROW_NUMBER() OVER (PARTITION BY co."CustomerId"
      ORDER BY co."OrderDate", co."Id", oi2."Id") for full determinism (multiple items in one
      order share OrderDate), (3) AlterColumn to NOT NULL. Verified against real Postgres: 3
      pre-existing OrderItems (2 sharing an OrderDate) backfilled to 1,2,3 in the correct tiebreak
      order. This is the exact sequencing v0.10.2 got wrong — confirmed both the migration's
      Up() and AppDbContextModelSnapshot.cs changed together.
    RC-5 — No new column on TreatmentSeries. TreatmentSeriesController.ToDto reads
      ts.OrderItem?.PackageNumber directly; confirmed .Include(ts => ts.OrderItem) was already
      present in both TreatmentSeriesRepository.GetActiveByCustomerIdAsync and GetByIdAsync.
  Files changed:
    backend/BeautyCareClinic.Domain/Entities/OrderItem.cs — PackageNumber property.
    backend/BeautyCareClinic.Infrastructure/Data/AppDbContext.cs — Fluent config (required, no index).
    backend/BeautyCareClinic.Infrastructure/Migrations/20260719105258_AddPackageNumberToOrderItems.cs
      (+ .Designer.cs) — new migration (RC-3/RC-4).
    backend/BeautyCareClinic.Infrastructure/Migrations/AppDbContextModelSnapshot.cs — PackageNumber
      added to the OrderItem block.
    backend/BeautyCareClinic.Api/Controllers/CustomerOrdersController.cs — RC-1 row lock + base
      query + per-item PackageNumber assignment + OrderItemDto construction.
    backend/BeautyCareClinic.Api/Controllers/TreatmentSeriesController.cs — ToDto reads
      ts.OrderItem?.PackageNumber (RC-5).
    backend/BeautyCareClinic.Application/DTOs/OrderDtos.cs — OrderItemDto.PackageNumber.
    backend/BeautyCareClinic.Application/DTOs/TreatmentSeriesDtos.cs — TreatmentSeriesDto.PackageNumber.
    backend/BeautyCareClinic.Tests/Integration/CustomerOrdersControllerPostgresTests.cs — new test
      asserting PackageNumber increments correctly across multi-item single order and separate
      sequential orders, read back from real Postgres.
    src/types/Order.ts, src/types/TreatmentSeries.ts — optional packageNumber field.
    src/features/customer/tabs/ActiveSeriesTab.tsx — #{s.packageNumber ?? idx + 1} (both quantity
      and timer series badges).
    src/features/customer/tabs/TreatmentHistoryTab.tsx — packageIndex={item.packageNumber ?? idx + 1}.
    docs/DATABASE_SCHEMA.md, docs/DOMAIN_MODEL.md, docs/API_SPECIFICATION.md — documented the new
      column/fields, numbering rules, and concurrency strategy.
    CHANGE_REQUESTS.md — CR-031 moved from Planned to Completed.
    FOLLOWUPS.md — FU-018 added (pre-existing, unrelated doc drift found in the same
      API_SPECIFICATION.md line: example uses "treatmentSeriesId" but the real DTO field is
      "seriesId" — not fixed, out of CR-031 scope).
  Live verification against running backend (:5000, restarted to load the rebuilt binary) +
  real Postgres:
    - GET /customers/{id}/orders and GET /customers/{id}/treatment-series for the same customer
      returned matching packageNumber values (2 and 3) for the same underlying package
      (orderItemId <-> seriesId pairs), confirming the two tabs will now agree.
    - POST a new order for that customer returned packageNumber 4 (continuing the max correctly);
      deleted afterward to restore state.
    - Fired 5 concurrent POSTs for the same customer: got 4,5,6,7,8 with no duplicates (RC-1 row
      lock verified under real concurrency); deleted afterward. Final DB state confirmed back to
      the original 3 rows numbered 1,2,3.
  Not touched: env.development.local, env.example, zipcheck/ (per explicit constraint).
  Not committed/tagged, per explicit instruction — implementation only, ready for code-reviewer.

2026-07-19 CR-031 correction — timer-series badge was missed in first pass
  Coordinator independently verified the CR-031 implementation before sending to code-reviewer
  and caught a real gap: src/features/customer/tabs/ActiveSeriesTab.tsx line 123 (quantity-series
  badge) was correctly updated to #{s.packageNumber ?? idx + 1}, but line 173 (timer-series badge)
  was left as the old #{idx + 1} — despite an earlier replace_all Edit call reporting "All
  occurrences were successfully replaced" for both. Confirmed via git diff that only one of the
  two occurrences had actually changed. This mattered specifically because the original user
  report that motivated CR-031 was about a timer-based package showing an inconsistent index, so
  the unfixed branch meant the feature did not actually resolve the reported symptom.
  Fix: line 173 changed to #{s.packageNumber ?? idx + 1}, matching line 123.
  Re-verified:
    - npx tsc -b --noEmit — same single pre-existing unrelated error (FU-016, RecordPaymentModal.tsx).
    - npx vitest run --config vitest.config.ts — 276/276 passing, no regressions.
    - Live check against the running backend (:5000) + real Postgres: the customer's existing
      timer-based series (orderItemId 6639ccf5-..., isTimerBased: true) returns packageNumber: 2
      from both GET /customers/{id}/treatment-series (ActiveSeriesTab's data source, both
      quantity- and timer-series branches now read s.packageNumber) and GET /customers/{id}/orders
      (TreatmentHistoryTab's data source) for the same item — confirmed consistent end-to-end.
  Files changed: src/features/customer/tabs/ActiveSeriesTab.tsx (one line).
  Not committed/tagged, per explicit instruction.

2026-07-19 CR-031 correction — added automated concurrency test for RC-1 (code-reviewer Medium finding)
  code-reviewer's full pass on CR-031 found no Critical/High issues (RC-1..RC-5 all confirmed
  correct, both ActiveSeriesTab badge fixes confirmed present) but flagged one Medium: the RC-1
  row-lock guarantee (no duplicate PackageNumbers under concurrent order creation for the same
  customer) had zero automated regression coverage — only manually/live verified earlier.
  Added: backend/BeautyCareClinic.Tests/Integration/CustomerOrdersControllerPostgresTests.cs —
  new test Create_ConcurrentRequestsForSameCustomer_AssignUniqueSequentialPackageNumbers. Fires 5
  truly parallel CustomerOrdersController.Create calls for the same customer via Task.WhenAll,
  each on its own AppDbContext (DbContext is not thread-safe — never shared across the parallel
  calls, matching the class's InitializeAsync connection setup), asserts the resulting
  PackageNumber set is exactly {1..5} with no duplicates/gaps, and re-verifies via a fresh
  untracked read straight from Postgres.
  Regression-proof: temporarily removed the "SELECT ... FOR UPDATE" row lock from
  CustomerOrdersController.Create and re-ran this test 3x — failed reliably all 3 times
  (Assert.Equal() Expected: 5 distinct values, Actual: 1 — all 5 concurrent calls read the same
  MAX(PackageNumber)=0 and collided on PackageNumber=1), proving the test genuinely exercises
  RC-1's guarantee rather than passing vacuously. Restored the lock (diff against the prior
  version of CustomerOrdersController.cs confirmed byte-identical to before this exercise).
  Flakiness check: ran the new test alone 5x and the full suite 3x with the lock restored — 5/5
  and 3/3 green (132/132 total), no flakes observed.
  Files changed:
    backend/BeautyCareClinic.Tests/Integration/CustomerOrdersControllerPostgresTests.cs — new
      test + _connectionString field (was a local var in InitializeAsync, promoted to a field so
      the new test can build additional per-call AppDbContext instances).
  Not committed/tagged, per explicit instruction.

2026-07-19 Code-review follow-up — added missing NotesController.Update regression coverage
  Code review of the payment/notes DateTime-Kind bug fix (PaymentsController.cs, NotesController.cs
  — both already fixed with DateTimeKind.Utc added to their ToDateTime calls) found a gap:
  NotesControllerPostgresTests.cs only regression-tested NotesController.Create. The Update action
  (~line 146) had the identical fix applied but zero coverage, so a future regression there
  (e.g. someone "simplifying" noteDate.ToDateTime(TimeOnly.MinValue, DateTimeKind.Utc) back to the
  2-arg overload) would not be caught by the suite.
  Added: Update_WritesNoteDateToRealPostgres_AndReturns200 in NotesControllerPostgresTests.cs.
  Creates a note via the controller's own Create path, then calls Update against the real Postgres
  AppDbContext with a new noteDate, asserts 200 OkObjectResult with the updated NoteDto, and reads
  the row back via a fresh AsNoTracking() query to prove the timestamptz column persisted the new
  value — same pattern as the existing Create test and CustomerOrdersControllerPostgresTests.cs.
  Also removed two dead Mock stubs flagged as P3 nits: currentUserMock.Setup(s =>
  s.GetCurrentUserRole()) in both PaymentsControllerPostgresTests.cs and
  NotesControllerPostgresTests.cs — neither PaymentsController nor NotesController calls
  GetCurrentUserRole() (confirmed via grep), only GetCurrentUserId()/IsManager().
  Files changed:
    backend/BeautyCareClinic.Tests/Integration/NotesControllerPostgresTests.cs — new Update test,
      removed dead GetCurrentUserRole() stub from the existing Create test.
    backend/BeautyCareClinic.Tests/Integration/PaymentsControllerPostgresTests.cs — removed dead
      GetCurrentUserRole() stub.
  Verified: dotnet build — 0 errors, 0 warnings. dotnet test (ConnectionStrings__DefaultConnection
  pointed at the dev Postgres via /home/runner/.microsoft/usersecrets/.../secrets.json,
  ASPNETCORE_ENVIRONMENT=Development) — 135/135 passing (134 pre-existing + 1 new).
  Not committed, per explicit instruction.

2026-07-19 Feature — editable Treatment.Notes via new PUT /api/v1/treatments/{id}
  Treatment.Notes was a real, persisted column with no way to set it after creation: no UI to
  enter one at record time, and TreatmentsController had no update endpoint at all (GET list, GET
  by id, POST create, DELETE only). Added the ability to add/edit a treatment's note from the
  Treatment History tab, at the same place photos are added (TreatmentModal.tsx), gated to the
  treatment's author or a Manager — mirroring the existing delete-treatment permission rule and
  the existing NotesController.Update pattern for the separate Note entity.
  Backend:
    - UpdateTreatmentRequest(string? Notes) added to TreatmentDtos.cs.
    - TreatmentsController.Update (PUT /api/v1/treatments/{id}): 404 if not found; 403 if caller
      is neither the treatment's author nor a Manager; 422 if Notes.Length > 5000. Unlike
      Note.Content, Treatment.Notes is optional — null/empty is explicitly allowed (clears the
      note) and does NOT throw, unlike NotesController's empty-content rejection. Only Notes is
      editable; TreatmentDate/DurationMinutes/TreatmentTypeId/TreatmentSeriesId are untouched.
    - ITreatmentRepository/TreatmentRepository gained UpdateAsync (previously only Note had one),
      following the exact NoteRepository.UpdateAsync style (_context.Treatments.Update +
      SaveChangesAsync).
  Frontend:
    - treatmentsApi.update(id, { notes }) + UpdateTreatmentRequest type added to treatmentsApi.ts
      and re-exported from api/index.ts, mirroring notesApi.update.
    - TreatmentModal.tsx: replaced the read-only note block with an editable one. Computes
      canEditNote from useAuth()'s currentUser vs treatment.userId/therapistId (author) or
      role === 'Manager', pulled directly via the useAuth() context hook (no prop drilling
      needed — TreatmentModal already renders inside the same provider tree as
      TreatmentHistoryTab, which already uses useAuth() the same way for its per-row delete
      button). Shows "הוסף הערה"/"ערוך הערה" only for author-or-manager; a plain read-only note
      (or nothing, if none) for everyone else. Edit reveals a <textarea> (maxLength=5000,
      styled like RecordPaymentModal.tsx's inputs) with שמור/ביטול; save calls
      treatmentsApi.update, then awaits useCustomer()'s refreshTreatments() (same mechanism
      TreatmentHistoryTab already uses after treatment delete) so the modal's treatment prop
      picks up the new note once the parent's treatments list refreshes. Errors use the same
      ApiRequestError → Hebrew-message / generic-fallback pattern as RecordPaymentModal.tsx.
  Tests:
    - Backend: 8 new cases in Phase010Tests.cs (existing InMemory-provider convention — auth
      checks simulated the same way DeleteTreatment_*/UpdateNote_* tests already do, plus direct
      TreatmentRepository.UpdateAsync persistence tests): author update persists, manager update
      persists, wrong-author 403 check, non-existent id (GetByIdAsync returns null), >5000 chars
      throws DomainValidationException, and null/empty notes do NOT throw (clearing is allowed).
    - Frontend: new TreatmentModal.test.tsx (mocks useCustomer/useAuth/treatmentsApi, following
      OrdersTab.test.tsx's hook-mocking convention) — 6 cases: edit affordance shown for author,
      shown for Manager on another therapist's treatment, hidden for a different non-manager
      therapist (note still shown read-only), "הוסף הערה" vs read-only-nothing when no note
      exists, save flow calls the API with the right payload and triggers refreshTreatments,
      5000-char client-side cap wired to the textarea.
  Verified:
    - dotnet build BeautyCareClinic.sln — 0 errors, 0 warnings.
    - dotnet test BeautyCareClinic.sln (ConnectionStrings__DefaultConnection + Development) —
      143/143 passing (135 pre-existing + 8 new).
    - npx tsc -b --noEmit — no new errors (only the known pre-existing FU-016
      RecordPaymentModal.tsx unused `currentUser` warning).
    - npx vitest run --config vitest.config.ts — 284/284 passing, 17 files (278 pre-existing + 6
      new in TreatmentModal.test.tsx).
    - Live-verified against the running dev stack (backend restarted on :5000 after the build,
      real Postgres): created a treatment as therapist1, then via real PUT calls confirmed
      author-update 200 (notes persisted in the response), a different therapist's update 403
      FORBIDDEN, manager-update-of-another's-treatment 200, non-existent id 404 NOT_FOUND,
      >5000-char notes 422 VALIDATION_FAILED, and clearing notes to null 200 with notes:null in
      the response. Deleted the scratch treatment afterward to leave the dev DB clean.
  Deferred / out of scope (per explicit instruction, not touched): Treatment Photos editing
  (already deferred to Phase 011 per the phase summary). No new follow-ups or backlog items
  identified — the feature closes the gap reported by the user cleanly within the ADR's stated
  scope.
  Not committed, per explicit instruction.

- Code-review follow-up fixes for the "edit treatment note" feature (PUT /api/v1/treatments/{id}),
  addressing 3 findings from the code-review pass:
  Finding 1 (High, docs/API_SPECIFICATION.md): Removed the stale "Treatment records are immutable
  after creation — no PUT endpoint" sentence (now false) and added a proper
  "### PUT /api/v1/treatments/{id}" section between POST and DELETE, matching the file's existing
  format (request shape `{ notes? }`, note that only `notes` is mutable, response codes
  200/403/404/422) — mirroring how `PUT /api/v1/notes/{id}` is documented in the same file.
  Finding 2 (High, missing real controller coverage): The 4 auth/length tests in Phase010Tests.cs
  (lines ~501-566) only re-implemented TreatmentsController.Update's logic locally as booleans —
  they never called the controller, so a missing [Authorize], wrong route, inverted &&/||, or
  broken guard in the real code would slip through. Added
  backend/BeautyCareClinic.Tests/Integration/TreatmentsControllerPostgresTests.cs, following the
  exact NotesControllerPostgresTests.cs / PaymentsControllerPostgresTests.cs pattern (real Npgsql
  AppDbContext, Database.MigrateAsync, self-contained seed/teardown, controller invoked directly
  with a Mock<ICurrentUserService>, AsNoTracking() read-back after each write). 6 cases: author
  updates own treatment (200, persisted), Manager updates another's (200, persisted), a different
  non-manager therapist attempts another's treatment (403 via the real ownership check, note NOT
  persisted), notes >5000 chars (DomainValidationException thrown, note NOT persisted), null notes
  clears an existing note (200, persisted as null), non-existent id (404). Had to attach a
  DefaultHttpContext via ControllerContext to each built controller — without it,
  ControllerBase.HttpContext is null and the controller's NotFound/403 branches NRE on
  HttpContext.TraceIdentifier when the action is called directly outside the ASP.NET pipeline (the
  existing Postgres test files never hit those branches, so this gap hadn't surfaced before).
  Added a comment above the 4 shallow Phase010Tests.cs tests pointing at the new integration file
  as the real regression guard; left the shallow tests in place per the finding's discretion.
  Finding 3 (Medium, TreatmentModal.test.tsx): The ">5000 chars" test only asserted
  `textarea.maxLength === 5000`, never actually got a >5000-char value into noteDraft or asserted
  the API wasn't called. Rewrote it to fireEvent.change the textarea to a 5001-char string
  (JSDOM doesn't enforce the maxLength attribute on programmatic changes), click "שמור", then
  assert the visible Hebrew error message ("ההערה חורגת מ-5000 תווים", the exact string
  handleSaveNote's length guard sets via setNoteError) renders and mockUpdate (treatmentsApi.update)
  was never called.
  Genuineness proof (same rigor as prior fixes this session): temporarily broke
  TreatmentsController.Update's ownership check (`&& !isManager` → `&& isManager`) — confirmed
  Update_ByManager_ForAnotherTherapistsTreatment_UpdatesNotes_AndPersists and
  Update_ByDifferentNonManagerTherapist_Returns403_AndDoesNotPersist both failed, then restored and
  confirmed all 6 new integration tests pass. Separately, temporarily disabled
  TreatmentModal.tsx's length guard (`if (false && noteDraft.length > NOTES_MAX_LENGTH)`) —
  confirmed the rewritten frontend test failed (error message never appeared), then restored and
  confirmed all 6 TreatmentModal.test.tsx cases pass.
  Verified:
    - dotnet build BeautyCareClinic.sln — 0 errors, 0 warnings.
    - dotnet test BeautyCareClinic.sln (ConnectionStrings__DefaultConnection + Development) —
      149/149 passing (143 baseline + 6 new in TreatmentsControllerPostgresTests.cs).
    - npx tsc -b --noEmit — no new errors; same 2 pre-existing errors as on main before this
      session's changes (FU-016 RecordPaymentModal.tsx unused `currentUser`, and a pre-existing
      TreatmentModal.test.tsx `treatmentPhotos` typing gap in the base Treatment fixture — both
      confirmed present via `git stash` before touching anything, so neither is a regression from
      this fix pass).
    - npx vitest run --config vitest.config.ts — 284/284 passing, 17 files (test count unchanged —
      the "rejects notes over 5000 characters" test was rewritten in place, not added).
  Deferred / out of scope: none identified beyond the ADR's stated scope. Did not touch Treatment
  Photos, NotesController.cs/PaymentsController.cs, OrdersTab.tsx, or the CR-031 packageNumber
  work, per explicit instruction.
  Not committed, per explicit instruction.
