# Copy to .env (gitignored) alongside docker-compose.yml and fill in the real values
# used by the running beautycare-postgres container. Never commit the real .env.
POSTGRES_USER=beautycare
POSTGRES_PASSWORD=
POSTGRES_DB=beautycareclinic
